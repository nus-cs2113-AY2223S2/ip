/**
 * Exception which occurs when the user tries to unmark a task which
 * was already not marked in the beginning.
 */

public class TaskAlreadyNotMarkedException extends Exception{
    //for tasks that have already been marked
}
