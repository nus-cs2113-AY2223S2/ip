/**
 This <code>IncompleteTaskException</code> class is thrown when a user attempts to perform an operation on a task
 without providing all the necessary information (e.g., attempting to add a deadline without specifying a deadline).
 */
public class IncompleteTaskException extends Exception {
}
