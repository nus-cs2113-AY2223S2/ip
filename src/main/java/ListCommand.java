/**
 * Represents a command to list all tasks in the tasklist.
 * A <code>ListCommand</code> object corresponds to a command with "list" type.
 */

public class ListCommand extends Command{
    public ListCommand(){
        super("list");
    }
}
