/**
 * Represents a command to exit the program.
 * A <code>ByeCommand</code> object corresponds to a command with "bye" type.
 */

public class ByeCommand extends Command{
    public ByeCommand(){
        super("bye");
    }
}