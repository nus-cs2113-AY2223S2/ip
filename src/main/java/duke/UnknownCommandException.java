package duke;

/**
 * A <code>UnknownCommandException</code> represents an Exception 
 * that happens when the user enters a command not recognized by the chatbot.
 */
public class UnknownCommandException extends Exception {
    
}
