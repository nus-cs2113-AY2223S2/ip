package duke.command;

/**
 * Represents a command. A <code>Command</code> object is
 * an abstract class that contains an abstract method
 * <code>execute()</code>
 */
public abstract class Command {

    public abstract void execute();
}
