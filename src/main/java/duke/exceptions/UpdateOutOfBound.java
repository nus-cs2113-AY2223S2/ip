package duke.exceptions;

/**
 * When update tasks, it catches the error where user input index is out of bound of the index of the array elements.
 */
public class UpdateOutOfBound extends Exception{
}
