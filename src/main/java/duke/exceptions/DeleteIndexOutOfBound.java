package duke.exceptions;

/**
 * When deleting tasks, it catches the error where user input index is out of bound of the index of the array elements.
 */
public class DeleteIndexOutOfBound extends Exception{
}
