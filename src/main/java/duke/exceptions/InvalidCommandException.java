package duke.exceptions;

/**
 *  InvalidCommandException is thrown when a user inputs an invalid command, such as a command which does not exist
 *  or a command with an invalid format.
 */
public class InvalidCommandException extends Exception {
}
