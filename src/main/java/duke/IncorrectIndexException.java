package duke;

/**
 * A <code>IncorrectIndexException</code> represents an Exception 
 * that happens when a method receives an index parameter that is out of bounds.
 */
public class IncorrectIndexException extends Exception {
    
}
