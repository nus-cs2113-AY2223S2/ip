public class DukeException extends Exception {
       // public DukeException(String error) {
         //   super(error);
        //}
        //System.out.println("OOPS!!! The description of a todo cannot be empty.");
    }
/*Minimal: handle at least the two types of errors shown in the example above.
Stretch goal: handle all possible errors in the current version.
 As you evolve Duke, continue to handle errors related to the new features added.
 */